function [C2C] = ExtractC2C(Data,args)
%UNTITLED2 Summary of this function goes here
%   Detailed explanation goes here

%Linear Spaced
    %Get TargetG
%Pulse Number Spaced
    %Get TargetG
    
%TargetG = GetTargetGfun(Data, MeanRefG, args.Spacing)

    % NormG = WeightConvfun(Data, args.WeightConversion);
            %w/ WV
                %ErrorMatrix = ErrorCalcfun(NormG, args.Spacing, args.Write_Verify_bool)
            %w/o WV
                %ErrorMatrix = ErrorCalcfun(NormG, args.Spacing, args.Write_Verify_bool)
    % DeltaG_G = WeightConvfun(Data, args.WeightConversion);
            %w/ WV
                %ErrorMatrix = ErrorCalcfun(NormG, args.Spacing, args.Write_Verify_bool)
            %w/o WV
                %ErrorMatrix = ErrorCalcfun(NormG, args.Spacing, args.Write_Verify_bool)
end

